export const viewFilter = Object.freeze({
  SHOW_EVENTS: 'SHOW_EVENTS',
  SHOW_PALS: 'SHOW_PALS'
});

export const notificationType = Object.freeze({
  NEW_EVENT: 'NEW_EVENT',
  NEW_MESSAGE: 'NEW_MESSAGE',
  NEW_PAL_INTEREST: 'NEW_PAL_INTEREST',
  NEW_EVENT_INTEREST: 'NEW_EVENT_INTEREST'
});

export const messageType = Object.freeze({
  EVENT: 'EVENT',
  GROUP: 'GROUP'
});

export const genger = Object.freeze({
  MALE: 'MALE',
  FEMALE: 'FEMALE',
  UNKNOWN: 'UNKNOWN',
  OTHER: 'OTHER'
});
